#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
int main()
{
    int i, j, k, l, rows;

    printf("Enter the number of rows:");
    scanf("%d",&rows);

    for(i=0; i<rows; i++)
    {
        for(l=0; l<=1; l++)
        {
            for(k=1; k<=5-(i+1); k++)
            {
                printf(" ");
            }

            if(l != 1)
            {
                for(j=1; j<= (2*i)+1; j++)
                {
                    printf("*");
                }
            }
        }

        printf("\n");
    }
    return 0;
}
*/
