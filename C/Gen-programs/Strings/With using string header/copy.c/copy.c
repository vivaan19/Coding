#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void main()
{
    char str1[10]= "Vivaan";
    char str2[10];
    char str3[10];
    strcpy(str2,str1); // Function of string copy
    puts(str2);

    /*Here the syntax is strcpy(destination,source)
    destination in what should be copied and source is copied in destination.*/

}
