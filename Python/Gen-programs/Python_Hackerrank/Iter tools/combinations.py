from itertools import combinations, permutations

s, k = input().split()

lst = list(s)
lst = sorted(lst)

s = "".join(lst)

for i in s:
    combi = 


# combinations are emitted in lexicographic sorted order. 
# So, if the input iterable is sorted, the combinations tuples will be produced in sorted order.



    